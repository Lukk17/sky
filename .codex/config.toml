# Codex CLI + IDE project-scoped MCP servers and preflight hooks.
# Codex performs no substitution here and treats every value literally. No token is written
# in this file: env_http_headers names the environment variable carrying an HTTP header, and
# env_vars whitelists the variables a stdio server inherits from the process that starts Codex.

[mcp_servers.context7]
url = "https://mcp.context7.com/mcp"

[mcp_servers.context7.env_http_headers]
CONTEXT7_API_KEY = "CONTEXT7_API_KEY"

[mcp_servers.grafana]
command = "uvx"
args = ["mcp-grafana"]
env_vars = [
    "GRAFANA_URL",
    "GRAFANA_SERVICE_ACCOUNT_TOKEN",
]

[mcp_servers.playwright]
command = "npx"
args = ["@playwright/mcp@latest"]

[mcp_servers.chrome-devtools]
command = "npx"
args = ["-y", "chrome-devtools-mcp@latest"]

[mcp_servers.redis]
command = "uvx"
args = [
    "--from",
    "redis-mcp-server@latest",
    "redis-mcp-server",
    "--url",
    "redis://localhost:6379/0",
]

# Preflight hooks. Same wording and gate as the other agent surfaces.

[[hooks.UserPromptSubmit]]

[[hooks.UserPromptSubmit.hooks]]
type = "command"
statusMessage = "Preflight gate"
command = "echo '{\"hookSpecificOutput\": {\"hookEventName\": \"UserPromptSubmit\", \"additionalContext\": \"PREFLIGHT: before code work, name the skills and subagents that own this task and invoke them, or say none apply and why. Delegate investigation, review and bounded implementation by default.\"}}'"
commandWindows = "echo {\"hookSpecificOutput\": {\"hookEventName\": \"UserPromptSubmit\", \"additionalContext\": \"PREFLIGHT: before code work, name the skills and subagents that own this task and invoke them, or say none apply and why. Delegate investigation, review and bounded implementation by default.\"}}"

[[hooks.SessionStart]]

[[hooks.SessionStart.hooks]]
type = "command"
statusMessage = "Task list"
timeout = 10
command = "cd \"$(git rev-parse --show-toplevel 2>/dev/null)\" && PY=$(command -v python3 || command -v python) && \"$PY\" -S -E .agents/hooks/task_list_sync.py --event sessionstart --format codex 2>/dev/null || exit 0"
commandWindows = "for /f delims^= %r in ('git rev-parse --show-toplevel 2^>nul') do @cd /d %r && python -S -E .agents/hooks/task_list_sync.py --event sessionstart --format codex 2>nul || exit 0"

[[hooks.Stop]]

[[hooks.Stop.hooks]]
type = "command"
statusMessage = "Formatting check"
timeout = 10
command = "cd \"$(git rev-parse --show-toplevel 2>/dev/null)\" && PY=$(command -v python3 || command -v python) && \"$PY\" -S -E .agents/hooks/no_ai_markers_check.py --format codex 2>/dev/null || exit 0"
commandWindows = "for /f delims^= %r in ('git rev-parse --show-toplevel 2^>nul') do @cd /d %r && python -S -E .agents/hooks/no_ai_markers_check.py --format codex 2>nul || exit 0"

[[hooks.SubagentStart]]

[[hooks.SubagentStart.hooks]]
type = "command"
statusMessage = "Preflight gate"
command = "echo '{\"hookSpecificOutput\": {\"hookEventName\": \"SubagentStart\", \"additionalContext\": \"PREFLIGHT: before code work, name the skills and subagents that own this task and invoke them, or say none apply and why. Delegate investigation, review and bounded implementation by default.\"}}'"
commandWindows = "echo {\"hookSpecificOutput\": {\"hookEventName\": \"SubagentStart\", \"additionalContext\": \"PREFLIGHT: before code work, name the skills and subagents that own this task and invoke them, or say none apply and why. Delegate investigation, review and bounded implementation by default.\"}}"

[[hooks.PreToolUse]]
matcher = "^(Bash|shell|apply_patch|Edit|Write|NotebookEdit)$"

[[hooks.PreToolUse.hooks]]
type = "command"
statusMessage = "Preflight gate"
timeout = 10
command = "cd \"$(git rev-parse --show-toplevel 2>/dev/null)\" && PY=$(command -v python3 || command -v python) && \"$PY\" -S -E .agents/hooks/preflight_gate.py --format codex 2>/dev/null || exit 0"
commandWindows = "for /f delims^= %r in ('git rev-parse --show-toplevel 2^>nul') do @cd /d %r && python -S -E .agents/hooks/preflight_gate.py --format codex 2>nul || exit 0"
