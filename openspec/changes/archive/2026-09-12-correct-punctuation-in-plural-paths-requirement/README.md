# correct-punctuation-in-plural-paths-requirement

Clear the one semicolon-joined clause in `api-versioning` that can be cleared without a design decision, the plural-noun path requirement, and record why the capability's other two requirements are left untouched.
