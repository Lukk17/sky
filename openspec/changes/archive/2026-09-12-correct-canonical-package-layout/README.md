# correct-canonical-package-layout

Correct the architecture capability's canonical package layout requirement to the package tree the four services actually carry and the ArchUnit rules actually enforce.
