# correct-punctuation-in-config-hygiene-spec

Clear the four semicolon-joined clauses in `spring-boot-hygiene`, correct the MySQL environment variable and the per-service springdoc claim, and report the two deliberate local-profile relaxations the restatement exposed.
