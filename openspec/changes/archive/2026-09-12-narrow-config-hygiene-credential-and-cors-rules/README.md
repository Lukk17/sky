# narrow-config-hygiene-credential-and-cors-rules

Exempt the local profile from the credential-default ban and narrow the cross-origin rule to forbid only a wildcard, so both spring-boot-hygiene rules match the configuration the project deliberately ships.
