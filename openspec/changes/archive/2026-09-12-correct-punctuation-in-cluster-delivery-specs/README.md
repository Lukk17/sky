# correct-punctuation-in-cluster-delivery-specs

Clear the four semicolon-joined clauses in `helm-charts` and `kubernetes-deployment`, replace the stale Auth0 callback example with the oauth2-proxy annotations the charts actually carry, and report the environment literals in chart defaults and the second apply path the restatement exposed.
