# resolve-notify-outbound-adapter-package-deviation

Rename sky-notify's `adapters.outbound.service` for the transport it adapts, back the name with rules that bite, and correct the two specification sentences the rename and the bound origin list make untrue.
