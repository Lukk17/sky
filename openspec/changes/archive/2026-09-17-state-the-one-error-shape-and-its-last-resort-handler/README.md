# state-the-one-error-shape-and-its-last-resort-handler

State the error contract every sky service already keeps, that an error answered with a body is an RFC 9457 problem detail, and the constraint that keeps it keepable, that the handler of last resort sits outside the advice set and so cannot outrank a deliberate mapping.
