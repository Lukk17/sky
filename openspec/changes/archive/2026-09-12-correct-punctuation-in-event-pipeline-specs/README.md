# correct-punctuation-in-event-pipeline-specs

Clear the four semicolon-joined clauses in `kafka-messaging` and `websocket`, correct the stale Auth0 issuer and the unconditional audience check in the STOMP requirement, and report the three producer properties the contract demands and no producer sets.
