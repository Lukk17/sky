# add-sky-common-handler-override-route

Finish the correction started by `correct-sky-common-module-shape`: say how a service replaces the shared REST exception handler, not only that it must not extend it.
