# state-the-generated-api-contract-and-correct-what-it-falsified

State that the three published API documents are produced from the services rather than written by hand, and correct the two merged requirements that the move falsified: the version literal a controller declares is now the literal a published path carries, and the empty-bodied 401 and 403 are documented by a shared annotation rather than by a hand-written contract.
