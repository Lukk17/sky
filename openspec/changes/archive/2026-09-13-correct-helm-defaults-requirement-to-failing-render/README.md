# correct-helm-defaults-requirement-to-failing-render

Retire the helm-charts requirement whose worked example still expects a placeholder render, and replace it with one that expects the failing render the charts now produce, names the two categories of literal nobody had listed, and says how the namespace is held out.
