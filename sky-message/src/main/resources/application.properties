# ==================================================================================================================
# SPRING / EUREKA setup

# service name
spring.application.name=sky-message

server.port=5553

eureka.client.service-url.defaultZone=${SERVICE_URL_DEFAULT_ZONE:http://localhost:8761/eureka}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

spring.mvc.locale-resolver=accept_header

spring.mvc.dispatch-options-request=true

# ==================================================================================================================
# DB setup

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/sky_message?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=Europe/Warsaw

# username and pass to stop autogenerated default pass
spring.datasource.username=root
spring.datasource.password=Lukk1234

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQL5InnoDBDialect

spring.jpa.show-sql=true

# ==================================================================================================================


lukk.helloWorld=Message service for Sky
