meta {
  name: Get Received Messages
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/msg/api/messages/received?page=0&size=20
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{bearerToken}}
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.content: isDefined
  res.body.totalElements: isDefined
}
