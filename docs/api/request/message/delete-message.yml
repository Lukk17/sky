info:
  name: Delete Message
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/msg/api/messages/1"
  auth:
    type: none
  headers:
    - name: Authorization
      value: "Bearer {{bearerToken}}"
    - name: Accept
      value: application/json

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
