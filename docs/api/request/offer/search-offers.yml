info:
  name: Search Offers (public)
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/offer/api/search"
  headers:
    - name: Content-Type
      value: text/plain
    - name: Accept
      value: application/json
  body:
    type: text
    data: Paris

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
