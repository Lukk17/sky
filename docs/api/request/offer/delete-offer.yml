info:
  name: Delete Offer
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/offer/api/owner/offers/42"
  auth:
    type: none
  headers:
    - name: Authorization
      value: "Bearer {{bearerToken}}"
    - name: Accept
      value: application/json

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
