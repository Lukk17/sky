info:
  name: Get Offer Owner
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/offer/api/offers/42/owner"
  auth:
    type: none
  headers:
    - name: Authorization
      value: "Bearer {{bearerToken}}"
    - name: Accept
      value: application/json

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
