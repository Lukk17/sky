---
apiVersion: v1
kind: Service
metadata:
  name: floci-service
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - name: api
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    component: floci
