# Production-environment overlay for sky-offer.
# See sky-booking/values-prod.yaml header for the rationale.

ingress:
  host: "skycloud.luksarna.com"
  tls:
    secretName: sky-tls-cert

deployment:
  image:
    pullPolicy: "IfNotPresent"
