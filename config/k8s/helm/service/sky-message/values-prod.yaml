# Production-environment overlay for sky-message.
# See sky-booking/values-prod.yaml header for the rationale.

ingress:
  host: "skycloud.luksarna.com"

deployment:
  image:
    pullPolicy: "IfNotPresent"
